# Structure

## directory

```
.
├── actions       # Actions를 모아두는 디렉토리입니다.
├── components    # Presentational components라고 불리는 컴포넌트들을 모아둡니다.
├── containers    # Container component라고 불리는 컴포넌트들을 모아둡니다.
└── reducers      # Reducers를 모아두는 디렉토리입니다.
```
